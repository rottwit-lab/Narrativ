{
  "previewPort": 3000,
  "healthPath": "/api/health",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GEMINI_API_KEY",
      "description": "Google Gemini API key (https://aistudio.google.com/apikey). Powers AI scriptwriting and cloud TTS. App boots without it and serves fallback acoustic previews.",
      "requiredAtBoot": false
    }
  ]
}
